# How to Play DEADSHIFT

Open Terminal and paste this:

```
git clone https://github.com/rpatino-cw/deadshift.git && cd deadshift && bash play.sh
```

That's it. It installs everything and opens the game.

Enter the server IP and room code your host gives you.
